  更好的提示词描述方式：

  ### 1. 明确"是什么"和"为什么"

  我想调整 routeList 结构，目标是：
  - 让 findRoutesByKey 能正确过滤包含 path: '' 的子路由
  - 保持路由的三级结构完整性

  ### 2. 用对比的方式描述（before → after）

  当前结构：
  /menuManagement/index → component 直接在父级

  期望结构：
  /menuManagement/index → 父级无 component，子路由 path: '' 承接 component，hideMenu: true

  ### 3. 说明关键约束

  约束：
  - path: '' 的子路由必须 hideMenu: true
  - 子路由的 key 需要与父级保持一致（让 findRoutesByKey 能匹配）
  - 如果父级有多个子路由需要加载，父级不能有 component

  ### 4. 列出所有需要调整的路由

  需要调整的路由：
  1. computingPowerPool（已有正确结构，跳过）
  2. dataManagement_dataSet（已有正确结构，跳过）
  3. menuManagement
  4. dictManagement

  ### 5. 给一个具体例子

  以 menuManagement 为例：

  当前：
  ```json
  {
    path: '/systemSettings/menuManagement/index',
    component: () => import('@/pages/systemSettings/menuManagement/index.vue')
  }
  ```
  期望改为：
  ```json
  {
    path: '/systemSettings/menuManagement/index',
    children: [{
      path: '',
      hideMenu: true,
      meta: { key: 'SYSTEMSETTINGS_MENUMANAGEMENT' },  // 与父级 key 相同
      component: () => import('@/pages/systemSettings/menuManagement/index.vue')
    }]
  }
  ```
  ### ---
  核心要点：把"你想要什么"和"为什么需要这样"都说清楚，AI 就不容易理解偏了。