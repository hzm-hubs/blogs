### app.component

```ts
//main.ts

import * as libComponents from '@/lib/index'

const app = createApp(App)
// 全局注册公用组件
Object.values(libComponents).forEach((component) => {
	app.component(`${component.__name}`, component)
})

app.mount('#app')
```