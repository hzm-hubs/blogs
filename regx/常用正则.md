```js
// 检测11位手机号
const phone = /^(13[0-9]|14[01456879]|15[0-35-9]|16[2567]|17[0-8]|18[0-9]|19[0-35-9])\d{8}$/;;
// 同时包含数字、大小写字母、符号 8位数以上
const high = /^(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z])(?=.*[!@#$%^&*()_+=\[\]{}|;:'",.<>\/?`~\\\-¥]).{12,30}$/;
// 实例子账户密码 同时包含数字、大小写字母,特定的特殊符号 12位数以上
const account= /^(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z])(?=.*[!#$%^&*()_+{}~-])[a-zA-Z0-9!%^&*()_+{}~-]{12,30}$/;
// 堡垒机
const jms= /^(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z])(?=.*[~!@#$%^*()_=\[\]{}.?])[a-zA-Z0-9!@#$%^*()_=\[\]{}.~?]{12,30}$/;
// 检测邮箱
const email = /^\w+([-+.]\w+)*@\w+([-.]\w+)*\.\w+([-.]\w+)*$/;
```

#### 前端代理匹配任意前缀

```
proxy: {
    [`${baseUrlPath}/apiServer`]: {
        target: `${apiUrl_LOCAL}`,
        changeOrigin: true,
        pathRewrite: { '^(.*?)apiServer': '' },
    },
},
```

根据传入的表达式进行验证
```
const regexpValidate = (regexp: string, value: any) => {
  let result = false
  try {
    result = new RegExp(regexp).test(value)
  } catch {}
  return result
}
```

[更多示例](https://www.jyshare.com/front-end/854/)